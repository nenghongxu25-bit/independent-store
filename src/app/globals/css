/* 珠宝展示格：强制图片 1:1 正方形且等大 */
.jewelry-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr); /* 手机端每行2个 */
  gap: 1rem;
  max-width: 1200px;
  margin: 2rem auto;
  padding: 0 1rem;
}

@media (min-width: 768px) {
  .jewelry-grid {
    grid-template-columns: repeat(3, 1fr); /* 电脑端每行3个 */
    gap: 2rem;
  }
}

.jewelry-item {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

/* 核心：强制所有容器尺寸相同 */
.image-container {
  aspect-ratio: 1 / 1; 
  overflow: hidden;
  background-color: #f9f9f9;
  border: 1px solid #eee;
}

.image-container img {
  width: 100%;
  height: 100%;
  object-fit: cover; /* 确保图片铺满且不拉伸 */
  transition: transform 0.5s ease;
}

.image-container img:hover {
  transform: scale(1.05);
}
